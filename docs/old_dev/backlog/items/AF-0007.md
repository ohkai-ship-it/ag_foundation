# AF-0007 — Core runtime skeleton v0 (interfaces + v0 playbook + stub skills) producing RunTrace
# Version number: v0.3

## Metadata
- **ID:** AF-0007
- **Type:** Foundation
- **Status:** Completed
- **Priority:** P0
- **Area:** Kernel
- **Owner:** Jacob
- **Completed in:** Sprint 01
- **Completion date:** 2026-02-24

## Problem
Need a working pipeline, but we must lock down interface style, method signatures, playbook definition strategy, and skill/tool behavior to avoid rework.

## Goal
Implement minimal sequence-only runtime with explicit interfaces and v0 implementations, using one hardcoded v0 playbook and a stub skill registry, producing a persisted RunTrace.

## Non-goals
- Branching/loops.
- Real LLM calls.
- Multi-agent delegation beyond linear steps.

## Acceptance criteria (Definition of Done)
- [ ] Interfaces are defined in `ag/core/interfaces.py` for: Normalizer, Planner, Orchestrator, Executor, Verifier, Recorder.
- [ ] Interfaces use `typing.Protocol` (preferred) or `abc.ABC` (acceptable) — consistent and documented.
- [ ] Exactly one hardcoded playbook `default_v0` exists in `ag/core/playbooks.py` (or equivalent).
- [ ] `default_v0` is linear; all steps default to `reasoning_mode='balanced'`.
- [ ] Executor uses a stub skill registry (`ag/skills/registry.py`) with at least one test skill (e.g., `echo_tool`).
- [ ] Happy-path integration test exercises all modules and persists a valid RunTrace.
- [ ] Failure-path integration test: missing skill or forced step error stops orchestration, records error, marks run failed.

## Implementation notes
- Playbooks are hardcoded in v0; defer YAML/JSON loading.
- Orchestrator is a simple loop.
- Verifier v0 can be basic; retries/repairs hardcoded to 0 or 1 (document).

## Risks
P1: interface drift. Mitigate by locking signatures early and enforcing with tests/type checks.

## PR plan (PR-sized)
1. PR (feat/runtime-skeleton): Add interfaces + v0 playbook + stub skills + runtime loop + integration tests.
