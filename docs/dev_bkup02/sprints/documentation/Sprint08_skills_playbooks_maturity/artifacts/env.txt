# Sprint 08 Review - Environment Evidence
# Date: 2026-03-09
# Executor: Jacob

## Python Environment
Python 3.14.0
pip 25.2

## Key Dependencies (pip freeze)
ag_foundation @ git+https://github.com/ohkai-ship-it/ag_foundation.git@58547dac43046a8a525d6c94ab5ed55589f558b1
click==8.3.1
openai==2.24.0
pydantic==2.12.5
pytest==9.0.2
pytest-cov==7.0.0
rich==14.3.3
ruff==0.15.2
typer==0.24.1

## CLI Verification
✅ `ag --help` works
✅ Commands: run, doctor, runs, ws, artifacts, skills, playbooks, config

## Manual Mode Gate
✅ AG_DEV=1 set in .env file (allows --mode manual)
✅ Manual mode correctly prints banner "DEV MODE: manual (LLMs disabled)"
✅ Gate loads from .env via python-dotenv (expected behavior)

Note: Manual mode gate cannot be tested without AG_DEV because .env is auto-loaded.
Would require removing .env or testing in isolated environment.
